var x = 2; // number
var y = "2"; // string

/*
    = assignment operator 
    == comparison operator, after doing any necessary type conversion
    === comparison with data type
*/

if (x === y) { // Comparison operator
    console.log("Both is same.");
} else {
    console.log("Both is not same.");
}
